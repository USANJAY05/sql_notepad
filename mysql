#!/bin/sh
# Wrapper script for the MySQL notebook environment

if [ "$#" -eq 0 ]; then
    set -- "help"
fi

if command -v python3 >/dev/null 2>&1; then
    python3 install.py "$@"
else
    python install.py "$@"
fi
